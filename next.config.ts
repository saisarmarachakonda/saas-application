import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['3ff273c619fe50.lhr.life', 'saikiran-nextgen-erp.serveo.net', 'saikiran-voc-erp.serveo.net', 'heavy-maps-chew.loca.lt'],
};

export default nextConfig;
